import React from 'react';
import { Text, View } from 'react-native';

const GGAuthenField = ({
    params,
}) => (
    <View>
        <Text>GGAuthenField</Text>
    </View>
);

export default GGAuthenField;
