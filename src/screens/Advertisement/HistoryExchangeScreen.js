import React from 'react';
import Container from '../../components/Container';

export const HistoryExchangeScreen = ({componentId}) => {
  return (
    <Container
      componentId={componentId}
      isScroll
      title="Lịch sử giao dịch"
      >

      </Container>
  );
};

export default HistoryExchangeScreen;
